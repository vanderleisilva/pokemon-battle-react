export const addInputs = (output) => {
    return {
        type: "ADD_INPUTS",
        output 
    }
}

export const addPlayer = (player) => {
    return {
        type: "ADD_PLAYER",
        player
    }
}